/// The name of the employee's capture key, used by every string that mentions it
/// (pill hover, menu item, Home empty state). Phase 03 binds the real key; the shell shows the sample.
enum CaptureKeyDisplay {
    static var name = "fn"
}
